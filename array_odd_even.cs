using System;
namespace ConsoleApp1
{
    class array_odd_even
    {
       /* public static void Main()
        {
            int[] arr = new int[10];
            for(int i=0;i<arr.Length;i++)
            {
                arr[i] = int.Parse(Console.ReadLine());
            }
            int even = 0;
            int odd = 0;
            for(int i=0;i<arr.Length;i++)
            {
                if(arr[i]%2==0)
                {
                    even++;
                }
                else
                {
                    odd++;
                }
            }

            int[] evenarr = new int[even];
            int[] oddarr = new int[odd];
            int e = 0, o = 0;
            for(int i=0;i<arr.Length;i++)
            {
                if (arr[i] % 2 == 0)
                {
                    evenarr[e] = arr[i];
                    e++;
                }
                else
                {
                    oddarr[o] = arr[i];
                    o++;
                }
            }
            Console.WriteLine("even number is :");
            foreach(int even1 in evenarr)
            {
                Console.WriteLine(even1);
            }
            Console.Write("odd number is :");
            foreach(int odd1 in oddarr )
            {
                Console.WriteLine(odd1);
            }
            
        }*/
    }
}
